apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - command: ../../manifest/setup.sh --manifest-dir ../../manifest
    namespaced: true
  - command: kubectl apply -f ../../manifest/global/pod-curl-target-gw.yaml -n 2 --create-namespace
