apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - command: kubectl scale deployment/target-deployment --replicas=2
    namespaced: true
  - command: kubectl rollout status deployment/target-deployment --timeout=400s
    namespaced: true
